
myf模板1.0，下划线表示还没改为自己风格，横线表示还没完成，+表示有测试过，
[X]template空模板
[ ]?bcc(poj2942)
[ ]_ac自动机(笔记本里）
[X]bignum（可以用给负数的大整数，但可能要写一份不需要大整数的）
[X]binary_tree（集成类的树状数组，下标为0～n-1）
[ ]_block_array（块状数组，不知道还有没有机会用上）
[X]_calculator（就是计算表达式的，功能略强大）
[X]_circle_k_union（求n个圆，被重叠了K次的面积）
[ ]?circle_tree(不明白，好像是圆包含关系)
[ ]?circle_union（不明白，好像是求圆与某矩阵的交或则并）
[ ]_combination_number（集成各种数论，坐等改进，我也有几种数论模板可以加上）
[ ]_cubic_solver（解三次方程，坐等改成自己风格）
[ ]?delaunay_triangulation（好像是什么delaunay三角剖分，不明觉厉）
[ ]_dijkstra(堆优化的，坐等改成自己风格)
[ ]_dinic（比较喜欢神牛模板的最大流，但是改成自己风格放那里也行）
[ ]?dlx_exact_cover（dancing link 不会用，精确覆盖？）
[ ]?dlx_multi_cover(dlx模糊覆盖？)
[ ]_dynamic_half_plane（半平面交，好像好容易用）
[ ]动态树（用clj和glz修改的那份）
[ ]_exgcd（各种与gcd有关代码，自己再添加修改吧）
[ ]fft(fft用之前A过题的那份，但是这份不知道要做什么，好像还有求原根做法)
[ ]_geometry（基本几何模板）
[ ]_geometry3d（3维空间几何模板）
[ ]_guassian(高斯消元，好像必须是n个方程n个变量的)
[ ]?half_plane（好像是半平面交）
[ ]_hash（字符串hash）
[ ]_heap(改成类模式)
[ ]_hungarian（匈牙利算法，改成自己风格，并用原先的匈牙利）
[ ]_integral（辛普森积分法，要弄清楚怎么使用，风格就不需要怎么改了）
[ ]?issa（好像是后缀数组，但是没看出来到底是DC3还是倍增的，但是代码量都太大）
[ ]_jump_list（lca的写法，改成自己风格，并集成类里面吧）
[ ]_kd_tree(kd树，但是只提供了查询区间，没有提供查询单点最近距离，还有少了knn算法（距离第K近的）)
[ ]_km（改成自己风格）
[ ]_kmp
[ ]_leftist_tree（左偏堆）
[ ]?limited_flow（有下界最大流，有源有汇，无源无汇的，都要去掌握其建图本质）
[ ]_linear_sequence（好厉害的数值计算，给定递推式，平方级计算）
[ ]?linear_transform3d（好像是三维空间的坐标变换，不太理解）
[ ]_manacher（似乎是最长回文子串算法）
[ ]_matrix（矩阵运算，包括加法、乘法和幂求和）
[ ]_max_sub_rect（最大子矩阵，好像可以推广到权值矩阵）
[ ]_min_cost_max_flow（最小费用最大流，也可以用神牛模板）
[ ]?newton（挂着牛顿的名，有取模运算，但是实在是看不懂要做什么）
[ ]?nim_product（好像是nim游戏的经典变型，可能是某题目的）
[ ]_polygon_intersection（三角剖分求面积交，可以学习三角剖分了）
[ ]_prime（基础的质数算法，我也将自己的数论相关全部集中起来吧，包括清华的那几种神奇写法）
[ ]_prime_random（素数的随机化算法，包括n^0.25分解质因数的作法）
[ ]?quadratic_residue(数论的二次剩余定理，不太理解，好好研究)
[ ]?quadratic_residue_solutions（接上，好好研究研究）
[ ]_range_tree（应该是二维空间，建立的区域树，支持插入删除点，和区域查询）
[ ]_rmq2d（二维rmq）
[ ]_scc（有向图强连通分量，但是tarjan算法也行，不用两次dfs，不过理解下思想不错）
[ ]_tarjan（离线lca问题的tarjan）
[ ]_treap(改成自己风格)
[ ]_2D Range Tree（区域树，支持加入点，和直接查询区间内有没有，和range_tree相似，）
[ ]_2dpoint（二维几何操作）
[ ]?All-Pairs Minimum Cut(Gusfield)(全局最小割，但不知道怎么实现和调用)
[ ]?All-Pairs Minimum Cut（全局最小割，还是不知道怎么调用）
[ ]_AROAD（最小K-度限度生成树)
[ ]求桥、割点都可以用tarjan白书的算法
[ ]_Chull3D O(n^2)（三维凸包算法）
[ ]_Chull3D O(n log n)（三维凸包算法）
[ ]_CutVertices on directed graph（有向图的割点，还是要试试怎么用）
[ ]?Dancing Links（舞蹈连，不知道怎么用）
[ ]_Dijkstra(STL)(好简洁的写法，我要学这个，最短路logN)
[ ]_Dijkstra（手写堆的）
[ ]_Dinic（神牛的sap好像不支持再增加边，继续增光，dinic好像可以）
[ ]_DiscreteLogarithm(Prime)（数论简单算法，有待整理，质数版本）
[ ]_DiscreteLogarithm（数论简单算法，非质数版本）
[ ]带花树模板还是用张建厂的那个
[ ]_ELFhash（经典字符串hash算法）
[ ]_Gauss（高斯消元，似乎只是xor方程的，兼用白书模板）
[ ]_GlobalMinCut（找最小割集算法）
[ ]_Graph Planarity（检测输入的图是否能平面化，超牛算法）
[ ]_HDU4010（动态树实例，操作有疑问的时候可以研究下）
[ ]_HighPrecision（高精度的加减乘除，必须改成自己风格，不然太丑了）
[ ]_Hopcroft-Carp（更加高效的二分图匹配算法，sqrt(n)*e）
[ ]_Hungary（二分图匹配的匈牙利算法，不用模板也可以）
[ ]_KM（二分图最大权匹配，KM算法）
[ ]_LCA_Online（在线LCA算法，写法很厉害）
[ ]_LCA-tarjan（离线LCA算法）
[ ]_LeftistHeap（左偏堆的数组写法）
[ ]_Jacobi Symbol（二次剩余系）
[ ]_Kth Shortest Loopless Path（无环第K小路径）
[ ]_Kth Shortest Path（可能是允许有环的第K小路径）
[ ]?LICS（好像是求最長公共上升子序列，但是程序略複雜）
[ ]_LP（超牛的解線性規劃的算法，回去試刷一下那道線性規劃）
[ ]Main.java（基本java模板，以後待用）
[ ]_MaxClique（最大團算法）
[ ]_maximum weighted matching in general graph（從名字看來說，似乎是說最大權一般圖匹配）
[ ]_MDST（最小樹形圖）
[ ]_MillerRabin（米勒測試算法）
[ ]_Mincost(fast)（看名字感覺應該是超快速的最小費用流）
[ ]_Mincost（一般的最小費用流）
[ ]_Minimum distance between 2 convex polygons（名字，應該是求兩凸包的最近距離）
[ ]_MinRepresentation（字符串的最小表示法）
[ ]！MIPT015（似乎是某道經典題，給點區間和點集，求最大的沒有包含障礙點的矩陣面積）
[ ]！MIPT016（似乎又是某道經典題，給定點集，求兩個不相交且不包含任何障礙點的矩陣，使得面積最大）
[ ]？NimProduct（nim遊戲的nim積運算，不太理解的運算，論文集裡確實有這個東西）
[ ]？NQueens（N皇后？方案數還是什麼？不太理解）
[ ]_Optimum Branchings O(N^2)（好像也是有向圖的最小生成樹）
[ ]_Optimum Branckings O(NM)（同上）
[ ]！PKU1741（經典題，樹的分治）
[ ]！PKU3016（经典题，将序列划分成K段单调序列，花费代价最小，左偏树）
[ ]！PKU3017（经典题，一个长度为n的数列，要求把这个数列划分为任意块，每块的元素和小于m，使得所有块的最大值的和最小，DP+单调队列）
[ ]！PKU3076(Sudoku)（经典题，数独dancing link）
[ ]！PKU3237(DT)（经典题，动态树做法）
[ ]！PKU3237(segtree)（经典题，树链剖分做法）
[ ]！POIZap（经典题，给定d,a,b，求0<=x<=a,0<=y<=b满足gcd(x,y)=d个数）
[ ]_Pollard（经典分解质因数算法，n^0.25）
[ ]_Prim(STL)（prime的STL库，优先队列做法）
[ ]_Prim（prime的手写堆算法）
[ ]_PrimeFilter（O（n）prime算法）
[ ]_Quadratic Residue（二次剩余，求解x^2=d(mod m)）
[ ]_RMQ（ST算法）
[ ]_roman（转罗马数字）
[ ]_SA O(n)（DC3的后缀数组，实用性不大）
[ ]？SA-IS（不明觉历）
[ ]！SGU261（经典题，给定p,k和a，求x^k=a(mod p)的所有解）
[ ]！SGU313（经典题，环上面，黑点白点匹配，使得总长度最小）
[ ]_TriangleCenters（求三角形的中心）
[ ]！URAL1627(Bit DP)（经典题，没看懂要干什么）
[ ]_by_lzq_1(经典题，动态添加点的凸包，并询问某点是否在里面)
[ ]_by_lzq_2（函数式treap）
[ ]_sam（后缀自动机）
[ ]_Geo（二维几何基础）
[ ]_Geo3D（三维几何基础）
[ ]_HalfPlaneIntersection（半平面交）
[ ]_LCT（link-cut-tree，动态树模板）
[ ]_Manacher（最长回文）
[ ]_Splay(clj)（clj版本的splay）
[ ]_mincostflow（神牛模板的传说很快最小费用流）
[ ]_maxflow（神牛模板的传说很快的最大流）
[ ]_myf_1（经典题，树的分治，距离之和>=K）

-------------------------------------------------------------------------------------------------------------

研究：数论的二次剩余定理，三角剖分，牛顿解法，网络流，两次dfs求scc，全局最小割

还要有的模板：KNN算法，带花树，动态KD树，FFT，那个论文子树操作的动态树，白书的2sat，白书的tarjan，白书的高斯消元，白书的，树剖，函数式线段树，树的分治

常用结论：数论的、组合数学的（回去看具体数学）、

